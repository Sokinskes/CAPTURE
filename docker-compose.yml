version: "3.9"
services:
  capture:
    build: .
    network_mode: host
    privileged: true
    environment:
      - ROS_MASTER_URI=http://localhost:11311
      - ROS_IP=127.0.0.1
    volumes:
      - /dev:/dev
      - ./data:/workspace/CAPTURE/data
      - /tmp/.X11-unix:/tmp/.X11-unix
